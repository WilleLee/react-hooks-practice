const NavBar = () => {
  return (
    <nav>
      <ul>
        <li>
          <a href={`hooks/use-state`}>useState</a>
        </li>
        <li>
          <a href={`hooks/use-input`}>useInput</a>
        </li>
      </ul>
    </nav>
  );
};

export default NavBar;
